services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ecospheres_dashboard
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
